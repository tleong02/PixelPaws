# PixelPaws — GPU-accelerated dependencies (recommended)
# Install: pip install -r requirements_gpu.txt
#
# Requires an NVIDIA GPU with CUDA 12.1.
# For CUDA 11.8: replace cu121 with cu118 in the torch index URL.

# Core scientific computing
numpy>=2.0.0
pandas>=2.0.0
scipy>=1.11.0

# Machine learning
scikit-learn>=1.3.0
xgboost>=3.0.0
shap>=0.43.0
statsmodels>=0.14.0

# Computer vision
opencv-python>=4.8.0
Pillow>=10.0.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# DeepLabCut HDF5 file support
h5py>=3.8.0
tables>=3.8.0

# Utilities
tqdm>=4.65.0

# PyTorch with CUDA 12.1 (GPU-accelerated brightness extraction)
--extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
torchvision>=0.15.0
